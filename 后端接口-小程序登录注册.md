# 后端接口：小程序专用登录 / 注册

## 方案（最终采用）

**不改动网站现有的 `/api/auth/login`、`/api/auth/register`**（它们继续只走 HttpOnly cookie，网站行为一字不变），而是**新增两个小程序专用接口**，在返回体里带上 token，供小程序用 `Authorization: Bearer <token>` 认证。

这两个接口已写入 `server/auth_server.py`（约 672–727 行）并部署，配套单元测试在 `tests/test_auth_points.py`。

## 接口契约

### 登录：`POST /api/auth/miniprogram-login`

请求体：
```json
{ "username": "账号", "password": "密码" }
```

成功返回（200）：
```json
{
  "token": "xxxxx",
  "user": { "username": "...", "name": "...", "points": 100, "role": "member", "must_change": false }
}
```

失败：`401` 账号或密码错误 · `429` 失败次数过多 · `400` 请求体非法。

### 注册：`POST /api/auth/miniprogram-register`

请求体：
```json
{ "username": "账号", "password": "至少6位", "display_name": "昵称(可选)" }
```

成功返回（200）：同上，带 `token` + `user`。
失败：`409` 账号已存在 · `400` 校验不通过 · `429` 注册过于频繁。

## 小程序侧对接

- `miniprogram/pages/login/login.js` 已指向这两个路径。
- 登录/注册成功后，把返回的 `token` 存进 `wx.setStorageSync('hq_token')`，后续所有请求由 `utils/api.js` 自动加上 `Authorization: Bearer <token>`。

## 验证记录

本地跑 `python3 -m unittest tests.test_auth_points` —— 7 项全过，含：
- 网站原 `/api/auth/login` 仍只返回 cookie、**无 token**（确认网站未受影响）
- `miniprogram-login` 返回的 token 可作 Bearer 通过 `/api/auth/me`
- `miniprogram-register` 返回 token 并成功建号

## 部署提醒

如果服务器上的 `auth_server.py` 已更新为含这两个接口的版本，重启即可生效：

```bash
sudo systemctl restart huangque-auth
sudo systemctl status huangque-auth   # active (running) 即成功
```
