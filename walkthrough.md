# Agent UI 组件库 Apple HIG 与图标规范升级完工报告

按照你的要求与指导，我们重新校准了设计主从关系：**全面以 Apple Human Interface Guidelines (Apple HIG) 为全局基石**，视觉图标以 **Apple SF Symbols 优先**，**oil-icon 仅作为苹果未覆盖的本土/业务图标的缺口补充**。已完成本地 Git 仓库、远端 Git 仓库与小程序体验版的全量对齐与发布。

---

## 核心设计规范体系与主从定位

```
┌─────────────────────────────────────────────────────────────┐
│                 全局基石：Apple HIG 设计规范                   │
│   iOS 系统蓝 (#007AFF) · Inset Grouped 分组卡片 · 44pt 触达   │
│   Apple checkmark.circle.fill 打勾圆环 · 系统级字体与层级     │
└──────────────────────────────┬──────────────────────────────┘
                               │
               ┌───────────────┴───────────────┐
               ▼                               ▼
  【优先】Apple SF Symbols 规范        【仅作补充】oil-icon 矢量补充
  - 文稿画卷: doc.text                 - 仅在遇到苹果符号库未覆盖
  - 声波律动: waveform                   的本土/行业专属业务概念时，
  - 人像剪影: person.crop.circle         选用 oil-icon 进行补齐
  - 视频胶卷: play.rectangle             - 统一采用系统标签黑/蓝渲染
  - 录音麦克: mic.fill
  - 微光推荐: sparkles
  - 播放控制: play.fill / pause.fill
```

---

## 核心设计与交互升级一览

### 1. 全局设计规范：坚决以 Apple HIG 为基石
- **改版前纠偏**：避免将第三方小众暖金色彩（`#8a6426`）误作为全局系统级标准的偏差。
- **改版后标准**：
  - **主交互色**：全量采用 Apple iOS 标志性系统蓝（`#007AFF`）；
  - **单选与多选打勾**：全面采用 Apple iOS `checkmark.circle.fill` 规范（未选中为 Apple 系统灰色 `2.5rpx` 细圆环 `#C7C7CC`；选中为实心 Apple 系统蓝 `#007AFF` 填充 + 纯白对勾 `✓` + 柔和投影）；
  - **卡片选中状态**：选中项外框为 `1.5rpx solid #007AFF`，底色微透淡蓝 `rgba(0, 122, 255, 0.04)`（`#F4F8FD`），保持 iOS 纯粹通透的高级质感；
  - **卡片容器**：遵循 Apple Inset Grouped 分组卡片规范，白色背景（`#FFFFFF`）配合 `1rpx solid rgba(60, 60, 67, 0.12)` 极细分隔线与柔和系统阴影；
  - **触达与反馈**：全量保持 44pt 适老化大触达区域，操作联动微信原生轻震动 `wx.vibrateShort({ type: 'light' })`。

### 2. 图标规范层级：SF Symbols 优先，oil-icon 补充缺口
- **标准交互图标全量对齐 SF Symbols**：
  - **口播文案卡**：采用 SF Symbols 风格 `doc.text`，标准粗细，系统文本色 `#1C1C1E`；
  - **音色卡**：采用 SF Symbols 风格 `waveform` 声波，系统文本色 `#1C1C1E`；
  - **数字人形象卡**：采用 SF Symbols 风格 `person.crop.rectangle`，系统文本色 `#1C1C1E`；
  - **视频与模板**：采用 SF Symbols 风格 `play.rectangle`，系统文本色 `#1C1C1E`；
  - **麦克风**：采用 SF Symbols 风格 `mic.fill`，系统文本色 `#1C1C1E`；
  - **播放控制**：采用 SF Symbols 风格 `play.fill` 与 `pause.fill`，在试听时呈现标志性 Apple 系统蓝 `#007AFF`；
- **oil-icon 补充机制**：仅在后续遇到特定本土电商/门店专属实体业务、苹果 SF Symbols 没有现成图标时，作为补齐扩充源，且色调融入整体系统环境。

### 3. 预备任务栏组件卡（Task Capsule Accordion Bar）
- **痛点解决**：以往一条对话同时吐出文案、音色、数字人 3 张长卡片，且无法收回，在手机屏上占据极长篇幅，造成视觉轰炸与中老年商户注意力分散。
- **改版后**：
  - **顶部任务胶囊栏**：在多张卡片上方新增常驻的任务胶囊栏 `.agent-task-bar`，包含 Apple 系统蓝任务编号标签（如 `任务 #01`）、任务主标题（`视频制作方案准备就绪`）、待选项目胶囊（`3 项待选`）、辅助说明与展开/收起切换按钮（`收起 ▴` / `展开 ▾`）；
  - **一键整体收纳**：轻触任务栏即可将下方的 3 张组件卡（1. 门店引流文案、2. 配音音色、3. 数字人形象）平滑收进任务栏中；
  - **收折自如**：用户需要调整时随时展开点选，选好后随时收缩折叠，保持聊天窗口干净通透。

### 4. 口播文案卡适老化重构：长文案支持「展开全文 ▾ / 收起 ▴」
- **改版后**：
  - 默认展示 3 行精华提炼，针对长文案（>50 字）自动提供显眼的 Apple 系统蓝「展开全文 ▾ / 收起 ▴」操作链接；
  - 配合原生轻震动平滑展开阅读全文，看懂了再放心点选；
  - 正则智能拆分版本标签（如「福利直给」、「痛点共鸣」），渲染为 Apple 风格浅灰圆角胶囊（`#F2F2F7`）。

### 5. 数字人形象卡（Avatar Pick）升级肖像画框与新增定制入口
- **改版后**：
  - **肖像画框**：升级为放大至 `108rpx × 108rpx`，微圆角 `20rpx`，Apple 灰微边框与纯矢量肖像线稿插画画框；
  - **新增定制入口**：卡片底部全面拉齐音色体系，采用 Apple iOS Tinted Button 浅色填充微描边风格，新增「＋ 录制 15 秒视频定制专属数字人（本人出镜）」；
  - **适老化采集引导**：配套平视、光照、15秒自然口播指引，支持「微信直接录制」与「相册上传正面视频」，彻底打通店主本人专属分身生成的闭环。

---

## Git 版本与小程序发布记录

### 1. Git 提交记录（本地与远端完全对齐）
- **最新 Commit**: [`f3b5e0b`](https://github.com/tang730125633/huangque-miniprogram/commit/f3b5e0b) (`feat(ui): 全面遵循 Apple HIG 设计规范，以 SF Symbols 为主，oil-icon 仅作为补充缺口`)
- **前序 Commit**: `efc0118` (`feat(ui): 新增预备任务栏组件卡，支持多卡片整体折叠与收缩`)
- **前序 Commit**: `89a1192` (`feat(ui): 数字人卡片新增定制入口，与音色克隆体系对齐`)
- **前序 Commit**: `45d2e85` (`fix(ui): 全面移除 Emoji 与字符图标，全量统一为纯矢量图标`)
- **Remote Push**: 已全量推送至 GitHub `origin/main`（本地与远端完全一致）。

### 2. 小程序体验版发布
- **最新体验版版本号**: **`v0.132`** (commit `f3b5e0b`)
- **AppID**: `wx23904aa2aa91f4d6`
- **发布状态**: 已通过微信官方开发者工具 CLI 自动上传并生效为最新体验版，真机直接扫码体验。

### 3. 全量自动化测试
运行工程全量 213 项单元测试：
```bash
node --test tests/*.test.js
```
- **测试总数**：`213`
- **通过数量**：`213`（100% PASS）
- **失败用例**：`0`
- **涵盖场景**：预备任务栏折叠展开、多卡勾选一次性提交、数字人定制入口方法绑定、文案展开收起与微震、音色播放器解耦与纯矢量图标等全部核心逻辑。
