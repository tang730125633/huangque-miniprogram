# 黄雀作图小程序 · 使用说明

这是网站「AI 作图（banana）」功能迁移到微信小程序的第一期 MVP。前端全部重写为小程序原生代码，后端复用你腾讯云上现有的 `https://huangquechuanmei.com` 接口。

---

## 一、你需要做的（按顺序）

### 1. 安装微信开发者工具

官方免费 IDE，下载地址：
https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

装好后用**你的微信扫码登录**（要用能管理这个小程序的微信号）。

### 2. 导入项目

打开微信开发者工具 →「导入项目」→ 目录选择：

```
WeChat project/miniprogram-project
```

（就是包含 `project.config.json` 的这个文件夹）

### 3. 填入你的 AppID

导入时会让填 AppID。把你**已注册小程序的 AppID** 填进去。

也可以之后在 `project.config.json` 里把 `"appid": "wxYOUR_APPID_HERE"` 改成你真实的 AppID。

### 4. 配置服务器域名白名单（很关键，否则接口全部调不通）

登录 **微信公众平台**（mp.weixin.qq.com）→ 你的小程序 →「开发」→「开发管理」→「开发设置」→「服务器域名」，在 **request 合法域名** 和 **downloadFile 合法域名** 里都加上：

```
https://huangquechuanmei.com
```

- `request` 域名：用于所有接口调用（登录、生成、轮询等）。
- `downloadFile` 域名：用于「保存到相册」下载结果图。

> 开发阶段想临时跳过：开发者工具右上角「详情」→「本地设置」→ 勾选「不校验合法域名…」，可在真机预览前先在模拟器里跑通。**正式发布前必须在公众平台配好白名单。**

### 5. 应用后端补丁（让登录能拿到 token）

见同目录 `后端补丁-登录返回token.md`。这是**小程序能登录的前提**——不做的话，登录会提示「后端未返回 token」。改动极小，只加两行。

### 6. 预览 / 真机测试

- 模拟器：直接在开发者工具里点「编译」。
- 真机：点「预览」，用手机微信扫码。

### 7. 发布

功能验收 OK 后，点开发者工具右上角「上传」→ 填版本号 → 到公众平台「版本管理」提交审核 → 审核通过后发布。

---

## 二、项目结构

```
miniprogram-project/
├── project.config.json        # 项目配置（AppID 在这里）
├── miniprogram/
│   ├── app.json               # 全局配置、页面注册、tabBar
│   ├── app.js                 # 全局数据（API base 地址）
│   ├── app.wxss               # 全局样式
│   ├── sitemap.json
│   ├── utils/
│   │   └── api.js             # 请求封装（拼域名 + 带 token + 401 跳登录）
│   └── pages/
│       ├── login/             # 登录 / 注册
│       ├── banana/            # 作图主页（提示词/引擎/比例/参考图/结果/历史）
│       └── profile/           # 我的（点数、退出登录）
├── 后端补丁-登录返回token.md
└── 使用说明-如何运行和发布.md   # 本文件
```

---

## 三、已实现的功能

- 账号密码**登录 / 注册**，token 存本地，未登录自动拦截。
- **作图**：提示词输入；5 个引擎（Nano Banana 2 / Pro、gpt-image-2、果肉生图、泽龙2）；比例 9:16 / 1:1 / 16:9 / 3:4；清晰度标准/高清；数量（按引擎上限）；实时**点数预估**。
- **图生图**：上传参考图（相册/拍照），自动转 base64 随请求发送。
- 提交后**轮询任务**，出图后展示结果，多图可点选切换。
- 结果图**保存到相册** / 点击看大图。
- **最近作品**历史（读 `/api/gen/history`），点图可预览。
- 点数展示与**不足提示**（402 → 提示充值），生成成功后自动刷新点数。
- 「我的」页显示账户点数、退出登录。

## 四、接口对应关系（与网页版一致）

| 功能 | 接口 | 方法 |
|---|---|---|
| 登录 | `/api/auth/login` | POST |
| 注册 | `/api/auth/register` | POST |
| 账户信息/点数 | `/api/auth/me` | GET |
| 退出 | `/api/auth/logout` | POST |
| 生成（Nano Banana） | `/api/gen/banana` | POST |
| 生成（gpt/果肉/泽龙） | `/api/gen/image` | POST |
| 查询任务 | `/api/gen/job/{id}` | GET |
| 历史 | `/api/gen/history?limit=12` | GET |

---

## 五、暂未包含 / 后续可加

- 网页版 banana 里的「局部修改（涂抹蒙版 inpaint）」较复杂，MVP 未做，后续可加。
- 充值页（recharge）：MVP 只做了点数不足提示，未接入小程序内支付。小程序内支付需单独接微信支付并走审核。
- 其他模块（视频、资产库、采集获客等）按规划分期迁移；获客采集类功能涉及第三方平台数据，上小程序前需先评估审核合规。

---

## 六、如果要改后端地址

编辑 `miniprogram/app.js` 里的 `apiBase`（末尾不要带 `/`），并同步更新公众平台的域名白名单。
